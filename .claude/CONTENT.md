# Development Log

Daily record of problems hit and how they were solved, for portfolio/interview reference.
One line per topic: problem → solution, key numbers/commits inline, resource link at the end.

Screenshots referenced here live in `.claude/screenshots/YYYY-MM-DD/`.

---

## 2026-09-23

- **Devlog setup**: started this log + moved the AWS plan in-repo to `.claude/plans/` for shareable, persistent tracking. → [plan](plans/aws-eks-load-test-deployment.md)
- **Bottleneck diagnosis**: code-read found the real AWS ceiling is zero gRPC channel pooling + no PgBouncer (not node/pod count) — reordered the plan to fix and prove both on the $0/month OCI cluster before touching AWS. → [auth-client.ts](../packages/grpc/src/auth-client.ts), [grpc-auth.guard.ts](../apps/api-gateway/src/auth/grpc-auth.guard.ts)
- **Phase 0 implementation**: added `channel-pool.ts` (pools clients across all 9 `*-client.ts` files, removed every `client.close()`) and a PgBouncer transaction-pooling proxy in front of Postgres (migrate Job deliberately bypasses it — advisory locks break under pooling). Commits `3b6fd90` (grpc), `a104d6c` (pgbouncer).
- **Phase 0c shipping**: fixed 3 unrelated infra hiccups while deploying to OCI — stale security-list IP from ISP rotation, a spurious CI matrix job for a non-deployable shared package, and PgBouncer crash-looping on the wrong `LISTEN_PORT`. Commits `e9c7254`, `8307600`.
- **Phase 0 verification (live traffic)**: proved both fixes work — `ValidateToken` averaged 6.5ms (was 100–500ms/hop), `pg_stat_activity` shows exactly 9 pooled backend connections instead of one-per-replica. → [jaeger-trace-validatetoken-pooled-4.41ms.png](screenshots/2026-09-23/jaeger-trace-validatetoken-pooled-4.41ms.png)
- **Plan redirect + OCI baseline**: user wants a 232→500→1000→2315 events/s target ladder proven on real AWS (not sizing derived from OCI); captured OCI's baseline as the "before" evidence — 9.6 req/s sustained, p95 333.96ms, no pod over 57m CPU (ceiling is node contention, not compute). → [grafana-oci-baseline-request-error-rate.png](screenshots/2026-09-23/grafana-oci-baseline-request-error-rate.png)
- **AWS credential setup**: initial `aws login` authenticated as root (not scoped, real risk for an interview-facing project) — created IAM user `terraform-admin` (AdministratorAccess + `SignInLocalDevelopmentAccess`), switched the browser console session to it, re-ran `aws login` for short-lived auto-rotating credentials instead of a static access key. Region `ap-south-1` (matches OCI's `ap-mumbai-1`, changed from the plan's original `us-east-1` default at user's request).
- **Phase 1 (AWS Budget)**: created a $20/month budget with 5 escalating alert thresholds (25%/50%/100%/200% actual, 100% forecasted), mirroring the OCI budget ladder — verified live, all 5 in `OK` state. → `platform-infrastructure/aws-budget/`, commit `542982f`
- **OCI cleanup**: found + committed 3 pending Terraform changes from an earlier session (`OPENAI_API_KEY` wiring, current IP) that were never applied — `terraform plan` confirmed exactly one clean resource change, applied it, restarted `ai-service` to pick up the real key. Commit `eb8081d` (platform-infrastructure).
- **Phase 2 (AWS Terraform fix)**: fixed the known bug — `backing_services` cluster modules weren't gated on `manage_floci` like everything else, and the `locals` block indexed `var.clusters["jenkins"/"argocd"/"observability"/"backing_services"]` unconditionally, so applying against `prod.tfvars` errored outright on the missing map keys (the three "jenkins/argocd/observability" locals turned out to be dead code, deleted; `backing_services_name` wrapped in `try()`). Along the way, also caught Terraform's AWS provider not understanding `aws login`'s new `login_session` credential format (bridged via `aws configure export-credentials`) and a pre-existing invalid security-group-rule description (`"ALB -> node NodePort"` — AWS disallows `>` — never caught before since that resource's `for_each` is empty on the Floci path). Added `create_nat_gateway`/`nodes_in_public_subnets` toggles for the burst-session no-NAT design, created `secrets.aws.tfvars` (gitignored, known values copied forward from the OCI side). **Verified**: `terraform plan` against `prod.tfvars` now produces a clean single-cluster, 82-resource plan (0 errors, 0 NAT/EIP resources) instead of erroring; Floci path confirmed unaffected. Commit `3803003` (platform-infrastructure).
- **Phase 3 (first real AWS provision + destroy rehearsal)**: the actual first `terraform apply` against real AWS surfaced 4 more real, previously-undiscoverable issues, each fixed live: (1) `aws configure export-credentials`-exported session tokens can expire mid-apply on a slow step — hit this when a 7-minute LuLu-blocked `tls_certificate` read (same class of issue as `k6` earlier — a new binary, `terraform`, never given a LuLu allow rule) ate into the token's window, leaving the node group's create call orphaned; refreshed credentials and re-applied, which needed a manual `terraform untaint` + later `state rm`/AWS-side `delete-nodegroup` + re-`import` dance to reconcile Terraform's state against what AWS actually did. (2) This account is on AWS's "Free Plan", which hard-blocks launching any non-free-tier EC2 instance type outright — `t3.medium` isn't eligible, so the node group's ASG silently retried failed launches for 27 minutes before AWS itself gave up and marked it `CREATE_FAILED`; user chose to stay on Free Plan rather than upgrade, switched to `m7i-flex.large` (free-tier-eligible, 2 vCPU/8GB, comparable to the OCI node) and capped `node_max_size` at 2 to stay under the account's 5-vCPU default quota. (3) 11 more Floci-only `docker_image`/`docker_volume` resources (jenkins/argocd/jaeger/otel_collector/prometheus/grafana images+volumes) turned out to be ungated too — same bug class as `backing_services`, fixed the same way. (4) EKS silently corrected a `version` drift (1.32→1.31) on its own during a cluster "modify" step — accepted, no config change needed. **Verified end to end**: cluster + node group + ALB all reached `ACTIVE`, `kubectl get nodes` showed both nodes `Ready` with real public IPs, then `terraform destroy` cleanly removed all 71 resources — confirmed directly against the AWS API (not just Terraform's exit code): 0 EKS clusters, EC2 instances `terminated`, 0 ALBs/NAT gateways/EIPs/VPCs/ECR repos/EBS volumes. Billing/Cost Explorer has a normal 8-24h reporting lag so the exact dollar figure isn't visible yet — scheduled a follow-up check. Commit `85bd13d` (platform-infrastructure).
