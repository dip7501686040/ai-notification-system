node_modules
dist
build
.turbo
coverage
pnpm-lock.yaml
readme.md

# Helm chart templates use Go template syntax ({{ }}) inside YAML/tpl files,
# which prettier's YAML parser can't parse as valid YAML.
infra/k8s/charts/**/templates/
