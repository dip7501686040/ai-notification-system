node_modules/
dist/
build/
generated/
.turbo/
.next/
next-env.d.ts
*.log
.env
.env.*
!.env.example
.DS_Store
coverage/
.venv/
__pycache__/
*.pyc

# Terraform and k8s deploy manifests now live in the platform-gitops repo,
# not here — see infra/README.md.

# Local-only dev config (Floci AWS profile) — machine-specific, not secrets
# but not meant to be shared/committed either.
infra/local/
